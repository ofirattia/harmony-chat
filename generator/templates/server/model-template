var mongoose     = require('mongoose');
var Schema       = mongoose.Schema;

var <%=capitalLetterModelName%>Schema   = new Schema({
	name: String
});

module.exports = mongoose.model('<%=capitalLetterModelName%>', <%=capitalLetterModelName%>Schema);